AWS_PROFILE=default
# AWS_COST_PROFILE=root-readonly     # Override aws_profile in config.yaml
# AWS_BEDROCK_PROFILE=dev-bedrock    # Override agent.profile in config.yaml
AWS_BEDROCK_REGION=us-east-1
AWS_COST_DB_PATH=./data/costs.duckdb
AWS_COST_CACHE_DIR=./data/cache
